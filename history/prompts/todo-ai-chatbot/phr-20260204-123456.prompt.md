---
id: phr-20260204-123456
title: Todo AI Chatbot Spec Creation
date: 2026-02-04
stage: spec
feature: todo-ai-chatbot
tags: [specification, ai, chatbot, todo]
---

# Todo AI Chatbot Spec Creation

## Original Prompt

```
You are an expert in spec-driven development using the Agentic Dev Stack. Based on the following requirements for Phase III: Todo AI Chatbot, generate a comprehensive specification document. Include all aspects such as objective, development approach, requirements, technology stack, architecture, database models, chat API endpoint, MCP tools specification, agent behavior specification, conversation flow, natural language commands, deliverables, OpenAI ChatKit setup & deployment, and key architecture benefits. Objective: Create an AI-powered chatbot interface for managing todos through natural language using MCP (Model Context Protocol) server architecture and using Claude Code and Spec-Kit Plus.

Development Approach: Use the Agentic Dev Stack workflow: Write spec → Generate plan → Break into tasks → Implement via Claude Code. No manual coding allowed. We will review the process, prompts, and iterations to judge each phase and project.

Requirements:
1. Implement conversational interface for all Basic Level features
2. Use OpenAI Agents SDK for AI logic
3. Build MCP server with Official MCP SDK that exposes task operations as tools
4. Stateless chat endpoint that persists conversation state to database
5. AI agents use MCP tools to manage tasks. The MCP tools will also be stateless and will store state in the database./Technology Stack:
- Frontend: OpenAI ChatKit
- Backend: Python FastAPI
- AI Framework: OpenAI Agents SDK
- MCP Server: Official MCP SDK
- ORM: SQLModel
- Database: Neon Serverless PostgreSQL
- Authentication: Better Auth

Architecture:
[Include the ASCII diagram here as text]
Database Models:
- Task: user_id, id, title, description, completed, created_at, updated_at (Todo items)
- Conversation: user_id, id, created_at, updated_at (Chat session)
- Message: user_id, id, conversation_id, role (user/assistant), content, created_at (Chat history) Chat API Endpoint:
- Method: POST
- Endpoint: /api/{user_id}/chat
- Description: Send message & get AI response
- Request: conversation_id (integer, optional), message (string, required)
- Response: conversation_id (integer), response (string), tool_calls (array) MCP Tools Specification:
- add_task: Purpose - Create a new task; Parameters - user_id (string, required), title (string, required), description (string, optional); Returns - task_id, status, title
- list_tasks: Purpose - Retrieve tasks; Parameters - user_id (string, required), status (string, optional: "all", "pending", "completed"); Returns - Array of task objects
- complete_task: Purpose - Mark as complete; Parameters - user_id (string, required), task_id (integer, required); Returns - task_id, status, title
- delete_task: Purpose - Remove a task; Parameters - user_id (string, required), task_id (integer, required); Returns - task_id, status, title
- update_task: Purpose - Modify task; Parameters - user_id (string, required), task_id (integer, required), title (string, optional), description (string, optional); Returns - task_id, status, title Agent Behavior Specification:
- Task Creation: When user mentions adding/creating/remembering, use add_task
- Task Listing: When user asks to see/show/list, use list_tasks
- Task Completion: When user says done/complete/finished, use complete_task
- Task Deletion: When user says delete/remove/cancel, use delete_task
- Task Update: When user says change/update/rename, use update_task
- Confirmation: Always confirm actions
- Error Handling: Handle task not found and errors gracefully Conversation Flow (Stateless Request Cycle):
1. Receive user message
2. Fetch conversation history from database
3. Build message array for agent
4. Store user message in database
5. Run agent with MCP tools
6. Agent invokes tools
7. Store assistant response
8. Return response
9. Server holds NO state Natural Language Commands:
- "Add a task to buy groceries" → add_task
- "Show me all my tasks" → list_tasks "all"
- And similar examples as provided.

Deliverables:
1. GitHub repo with /frontend, /backend, /specs, database migrations, README
2. Working chatbot that manages tasks via NLP, maintains context, handles errors, resumes conversations. OpenAI ChatKit Setup & Deployment:
[Include domain allowlist steps, environment variables]

Key Architecture Benefits:
- MCP Tools: Standardized interface
- Single Endpoint: Simpler API
- Stateless Server: Scalable, resilient
- Tool Composition: Chain tools

Output the specification in a structured Markdown format. Important LLM decision for this project:
• We are NOT using native OpenAI models (gpt-4o, etc.)
• Instead, use Google Gemini models via the official OpenAI-compatible endpoint
• Use this exact client configuration in the backend:

```python
import asyncio
from openai import AsyncOpenAI
from agents import Agent, OpenAIChatCompletionsModel, Runner, set_tracing_disabled

gemini_api_key = "YOUR_GEMINI_API_KEY_HERE"   # will come from env

client = AsyncOpenAI(
    api_key=gemini_api_key,
    base_url="https://generativelanguage.googleapis.com/v1beta/openai/",
)

set_tracing_disabled(True)

# Then in agent creation:
model=OpenAIChatCompletionsModel(
    model="gemini-2.5-flash",   # or gemini-2.5-pro if allowed
    openai_client=client,
)
```
```

## Response Summary

Successfully created a comprehensive specification document for the Todo AI Chatbot feature based on the provided requirements. The specification includes:

- User scenarios and testing requirements with prioritized user stories
- Functional requirements covering natural language processing, task management, and conversation context preservation
- Key entities definition for conversations, messages, tasks, and MCP tools
- Success criteria with measurable outcomes for user satisfaction and system performance
- Quality validation checklist confirming the specification meets standards

The specification was created in the file specs/1-todo-ai-chatbot/spec.md with appropriate validation checklist at specs/1-todo-ai-chatbot/checklists/requirements.md.

## Outcome

Specification document created successfully, ready for the planning phase using the Agentic Dev Stack workflow.